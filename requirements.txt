# Unsloth and dependencies
# Note: Install Unsloth from git for latest version
# pip install "unsloth[cu121-torch230] @ git+https://github.com/unslothai/unsloth.git"

# Core dependencies (installed with Unsloth)
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.24.0
peft>=0.7.0
bitsandbytes>=0.41.0
trl>=0.7.0
xformers<0.0.27

# Additional utilities
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# HuggingFace Hub
huggingface-hub>=0.19.0

# Jupyter (if running locally)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Optional: For video processing
# opencv-python>=4.8.0

# Platform-specific notes:
# - Google Colab: Run installation cell in each notebook
# - Vertex AI Workbench: Run setup.sh for automated installation
# - Kaggle: Similar to Colab, run installation cells
# - SageMaker: May need additional CUDA dependencies
# - Local Jupyter: Ensure CUDA 12.1+ and PyTorch 2.3+ are installed
